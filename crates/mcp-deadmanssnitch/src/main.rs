#![forbid(unsafe_code)]
#![allow(
    clippy::multiple_crate_versions,
    reason = "rmcp requires proc-macro consumers on both supported syn major lines"
)]
#![doc = "MCP server executable scaffold."]

fn main() {
    eprintln!("mcp-deadmanssnitch phase 0 scaffold");
}
