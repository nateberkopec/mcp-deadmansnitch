# Twin fidelity guardrail

The twin shares no production code with the client or server. Implement observed boundary behavior, including awkward status codes and legacy fields. Every fidelity change adds a differential case. Determinism comes from a frozen clock, seeded tokens, and reset between scenarios.
