# Dependency guardrail

Use the workspace dependency table and the pinned Rust toolchain. Permit MIT and Apache-2.0 dependencies from known registries. Avoid duplicate versions. Dependency changes run fmt, clippy, tests, docs, and cargo-deny.
