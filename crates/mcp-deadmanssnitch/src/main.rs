#![forbid(unsafe_code)]
// rmcp 3.2.0 currently brings proc-macro consumers on both supported syn major lines.
#![allow(clippy::multiple_crate_versions)]
//! MCP server executable scaffold.

fn main() {
    eprintln!("mcp-deadmanssnitch phase 0 scaffold");
}
