# Automation runbooks

Each background loop is delegated to its named runbook. Runbooks define authority and retirement; loop prompts contain no hidden policy. Automation-specific state belongs beside its runbook rather than at the top of `docs/`. Pull requests merge after CI passes and an independent reviewer has no further feedback; factory changes additionally require current-head human approval.
