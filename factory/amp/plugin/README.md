# Factory plugin

Amp loads this directory through `.amp/plugins/factory`. It supplies the issue worker, reviewer, conflict resolver, chief-of-staff launcher, and deterministic merge gate described in `factory/readme.md`.
