#!/usr/bin/env bash
set -euo pipefail
export PATH="$HOME/.local/bin:$PATH"
command -v mise >/dev/null
mise trust --yes
command -v fish >/dev/null
mise exec -- rustc --version
mise exec -- node factory/check.mjs
mise exec -- npm --prefix factory/amp/plugin run typecheck
