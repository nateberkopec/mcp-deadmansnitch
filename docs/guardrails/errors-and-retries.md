# Errors and retries guardrail

Errors identify a stable kind and a recovery action. Frame upstream text as untrusted. Never expose credentials. Bound requests to three attempts, ten seconds each, and one MiB responses; honor retry advice without unbounded sleeps.
