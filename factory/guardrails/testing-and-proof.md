# Testing and proof guardrail

Match proof to the claim. Unit-test pure state, test client behavior against the twin, and test MCP behavior through an MCP client. The twin is the only HTTP double. Live API checks are opt-in differential runs only. Relevant skipped checks must be reported.
